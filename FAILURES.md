# FAILURES

Every mistake an agent makes that a human (or the reviewing model) had to correct.
One line each, newest at the bottom. This log is also seed data for the product itself.
Format: `YYYY-MM-DD | driver (claude-code/codex) | what went wrong | the correction`

2026-07-23 | claude-code | tsconfig.json had `rootDir: src`, so `tsc --noEmit` failed on tests/cli.test.ts (outside rootDir) | removed the rootDir restriction since noEmit made it unnecessary
2026-07-23 | claude-code | eslint.config.js had no Node globals and applied type-aware parsing (parserOptions.project) to tsup.config.ts/vitest.config.ts, which aren't in tsconfig's include | added a node globals block for all files and a separate override for *.config.ts without the project option
2026-07-24 | claude-code | discoverSessions() computed totalProjects before applying the --project filter, so `debug:sessions --project zzz-nonexistent` printed "2 project(s), 0 session(s)" instead of 0 | moved the totalProjects count to after the filter is applied; caught by manually running the CLI against real transcripts, not by the unit tests
2026-07-24 | claude-code | first cut of the tool_error heuristic pattern-matched ("error"/"failed"/etc.) against ALL tool_result content, so Read-tool dumps of PRD.md/START-HERE.md/PLAN.md got flagged as errors just for containing that prose (e.g. PRD's own "Error handling requirements" section) — caught by reading real `debug:signals` output, not the unit tests | now only apply free-text pattern matching to command-execution tools (Bash); other tools only count as tool_error via the structured `is_error: true` flag. Residual, accepted-for-now noise: a Bash tool_result whose output happens to quote another process's CLI flag containing "error" (e.g. curl's own `--show-error` flag inside a `ps aux` dump) can still false-positive — narrow edge case, left for Step C's LLM summarization (task 2.1) to discard rather than hand-tuning the regex further
2026-07-24 | claude-code | added a `setTimeout`-based backoff helper to llm.ts (task 3.3) without adding `setTimeout` to eslint.config.js's node-globals list, so lint failed with no-undef | added setTimeout to the globals block
2026-07-24 | claude-code | same mistake again one task later: task 4.1's spinner.ts used `setInterval`/`clearInterval` without adding them to eslint's node-globals list — this is now a recurring pattern (3rd time adding a Node builtin without updating the globals list), exactly the kind of thing harnessc itself is meant to catch | added setInterval/clearInterval to the globals block; if a 4th instance happens, switch eslint.config.js to the `globals` npm package's full `node` set instead of hand-listing builtins one at a time
2026-07-24 | claude-code | task 4.2's fix-preview demo config scoped the custom rule with `files: ["bad-example.js"]`, which silently never matched (relative pattern vs. the path ESLint actually resolved when invoked with `--config` from the repo root) — eslint ran, found 0 problems, exited 0, and looked like success while the rule never applied at all | removed the `files` restriction since this config is only ever invoked with one explicit target file anyway; caught by actually running `npm run demo:fix-preview` and noticing the suspicious silence, not by assuming a clean exit meant it worked
